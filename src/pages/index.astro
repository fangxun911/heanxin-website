---
import BaseLayout from "../layouts/BaseLayout.astro";
import CapabilityStrip from "../components/CapabilityStrip.astro";
import HomeHero from "../components/HomeHero.astro";
import HomeQuoteCta from "../components/HomeQuoteCta.astro";
import HomeSolutionCard from "../components/HomeSolutionCard.astro";
import T from "../components/T.astro";
import home from "../content/home.json";
import media from "../content/media.json";

const assets = media.assetMap;
---

<BaseLayout
  title="塑胶注塑与模具定制制造商"
  description="禾安鑫专注塑胶注塑、模具开发、嵌件注塑与五金塑胶组合件，为电子、汽车、医疗等行业提供 OEM / ODM 一站式定制制造服务。"
>
  <HomeHero
    title={home.hero.title}
    subtitle={home.hero.subtitle}
    image={assets[home.hero.image]}
    primaryCta={{ label: home.hero.primaryCta, href: "/contact#quote", icon: "document" }}
    secondaryCta={{ label: home.hero.secondaryCta, href: "/contact", icon: "mail" }}
  />

  <section class="container-page py-14 sm:py-16">
    <div class="border-line flex items-end justify-between gap-8 border-b pb-6">
      <h2 class="font-display text-brand-800 text-3xl font-bold sm:text-4xl">
        <T text={home.coreSolutions.title} />
      </h2>
      <span class="bg-ember-500 hidden h-1 w-20 sm:block" aria-hidden="true"></span>
    </div>
    <div class="mt-8 grid gap-5 md:grid-cols-2 2xl:grid-cols-4">
      {
        home.coreSolutions.items.map((item) => (
          <HomeSolutionCard
            icon={item.icon}
            title={item.title}
            desc={item.desc}
            image={assets[item.image]}
            href={item.href}
          />
        ))
      }
    </div>
  </section>

  <section class="border-line section-tint border-y">
    <div class="container-wide py-10 sm:py-12">
      <CapabilityStrip items={home.capabilityStrip.items} />
    </div>
  </section>

  <HomeQuoteCta {...home.quoteCta} />
</BaseLayout>
